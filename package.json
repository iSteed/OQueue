{
  "name": "oqueue",
  "version": "0.1.0",
  "private": true,
  "description": "OGame build queue Tampermonkey userscript",
  "scripts": {
    "test": "node --test test/*.test.js",
    "build": "node build.js"
  }
}
